@model IEnumerable<MOM.Models.StaffListVM>
@{
    ViewData["Title"] = "Staff List";
}

<div class="pagetitle">
    <h1>Staff</h1>
    <p class="text-muted">Manage staff members</p>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Home</a></li>
            <li class="breadcrumb-item">Staff</li>
            <li class="breadcrumb-item active">List</li>
        </ol>
    </nav>
</div>

<section class="section">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-transparent border-0 d-flex flex-wrap justify-content-between align-items-center gap-2 py-3">
                    <h5 class="card-title mb-0 d-flex align-items-center">
                        <i class="bi bi-person-badge me-2 text-primary"></i> Staff
                    </h5>
                    <div class="d-flex gap-2">
                        <form class="d-flex" method="get">
                            <input type="text" name="SearchText" class="form-control form-control-sm me-2" placeholder="Search staff..." value="@ViewBag.CurrentSearchText">
                            <button type="submit" class="btn btn-light btn-sm"><i class="bi bi-search"></i></button>
                        </form>
                        <a asp-action="Create" class="btn btn-primary btn-sm">
                            <i class="bi bi-plus-lg me-1"></i> Add Staff
                        </a>
                    </div>
                </div>
                <div class="card-body pt-0">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover align-middle mb-0">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Name</th>
                                    <th>Department</th>
                                    <th>Email</th>
                                    <th>Mobile</th>
                                    <th class="text-center">Mtgs</th>
                                    <th class="text-end">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model != null && Model.Any())
                                {
                                    int count = 1 ;
                                    foreach (var item in Model)
                                    {
                                        <tr>
                                            <td><span class="fw-medium">@count</span></td>
                                            <td>@item.StaffName</td>
                                            <td>@item.DepartmentName</td>
                                            <td>@item.EmailAddress</td>
                                            <td>@(item.MobileNo ?? "—")</td>
                                            <td class="text-center">@item.TotalMeetings</td>
                                            <td class="text-end">
                                                <div class="btn-group">
                                                    <a asp-action="Details" asp-route-id="@item.StaffID" class="btn btn-outline-info btn-sm" title="Details">
                                                        <i class="bi bi-eye"></i>
                                                    </a>
                                                    <a asp-action="Update" asp-route-id="@item.StaffID" class="btn btn-outline-primary btn-sm" title="Edit">
                                                        <i class="bi bi-pencil"></i>
                                                    </a>
                                                    <a href="javascript:void(0);" onclick="confirmDelete('@Url.Action("Delete", new { id = item.StaffID })')" class="btn btn-outline-danger btn-sm" title="Delete">
                                                        <i class="bi bi-trash"></i>
                                                    </a>
                                                </div>
                                            </td>
                                        </tr>
                                        count++;
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="7" class="table-empty-state text-center py-4 text-muted">No staff found. Add your first staff member above.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
